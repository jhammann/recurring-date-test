.ui.breadcrumb
  = link_to "Dates", :dateslots
  %i.right.arrow.icon.divider
  .active.section Overview
%h1 Overview
%table.ui.table.segment
  %tr
    %th Activiteit
    %th Datum
    %th Van
    %th Tot
    %th Link
  - @dateslots.each do |dateslot|
    %tr
      %td= dateslot.title
      %td= first_event(dateslot, dateslot.schedule)
      %td= dateslot.start_time.strftime("%H:%M")
      %td= dateslot.end_time.strftime("%H:%M")
      %td
        = link_to dateslot, :class => "mini ui button" do
          %i.info.icon
          Bekijk
= link_to :dateslots, :class => "ui animated tiny button" do
  .visible.content Terug naar overzicht
  .hidden.content
    %i.left.arrow.icon